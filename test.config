
{extend, "rebar.config"}.
{extend, "deps/build_config/test.config"}.

{deps, [
    {meck, ".*",
        {git, "https://github.com/eproxus/meck.git"}}
]}.

{qc_no_cover, ".*_tests"}.
{qc_src_dir, "props"}.
